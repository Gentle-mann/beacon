REACTOR_API_KEY=replace_with_your_reactor_api_key
GEMINI_API_KEY=replace_with_your_gemini_api_key

# The root preview works without API keys. Enable live only after the team
# validates the patient prompts and coordinates the single model slot.
# Seed 2026 is the team's current selection in lib/scene.ts (PR #4).
BEACON_LIVE_ENABLED=false
BEACON_LOCKED_SEED=2026
